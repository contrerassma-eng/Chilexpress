{
  "name": "botiquin",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Inventario del botiquin de casa: escanea, compra suma stock, consumo resta stock. PWA sobre Cloudflare Workers + D1.",
  "scripts": {
    "dev": "vite",
    "dev:api": "wrangler dev",
    "build": "vite build",
    "preview": "vite preview --host",
    "deploy": "npm run build && wrangler deploy",
    "db:init": "wrangler d1 execute botiquin --remote --file=./migrations/0001_init.sql && wrangler d1 execute botiquin --remote --file=./migrations/0002_zonas.sql && wrangler d1 execute botiquin --remote --file=./migrations/0003_inventarios.sql",
    "test": "node test/logica.mjs",
    "test:api": "node test/api.mjs"
  },
  "dependencies": {
    "@zxing/browser": "^0.1.5",
    "@zxing/library": "^0.21.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11",
    "vite-plugin-pwa": "^0.21.1",
    "wrangler": "^4.20.0"
  }
}
